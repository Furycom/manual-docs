# SESSION LATEST — 2026-01-23

## Résumé
- Création de la fonction SQL `public.exec_sql_write(query text)` dans Supabase Postgres (container `supabase-db`).
- Le pipeline RAG embeddings fonctionne: `bruce_rag_embed.py --limit 50` = 50/50 OK.
- SSH sans mot de passe depuis `furymcp` vers `supabase` est fonctionnel (clé installée via `ssh-copy-id -f`).
